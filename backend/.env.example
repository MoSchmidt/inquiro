ENVIRONMENT=dev

# --- Database Configuration ---
POSTGRES_USER=inquiro
POSTGRES_PASSWORD=inquiro
POSTGRES_DB=inquiro_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# --- Security & Authentication ---
# JWT_SECRET_KEY: Used for signing access tokens.
# Generate a unique 64-character hex string using: openssl rand -hex 32
JWT_SECRET_KEY=3e3cc0b2f8b54b3d6aa597faccb2f9c41b19b1c24befe29661bafbd42e3afe3e

# SAFETY_CANARY: A unique security token for internal validation.
# Generate a unique base64 string using: openssl rand -base64 32
SAFETY_CANARY=BNZe4fAKVj/DZ/atHZZaVZxpyZGDZt+TqH0sT5J6AMY=

# --- AI & LLM Services ---
# Required for semantic ranking and paper analysis features.
# Get your key at: https://platform.openai.com/
OPENAI_API_KEY=your_openai_api_key_here
