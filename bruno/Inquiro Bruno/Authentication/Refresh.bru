meta {
  name: Refresh
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/refresh
  body: json
  auth: inherit
}

body:json {
  { 
    "refresh_token": "{{refresh_token}}" 
  }
}

tests {
  let data = res.body;
  bru.setEnvVar("access_token", data.access_token);
}
